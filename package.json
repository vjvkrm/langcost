{
  "name": "langcost-monorepo",
  "private": true,
  "version": "0.16.0",
  "description": "Cost intelligence and fault attribution for AI agent systems",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/db build && bun run --cwd packages/analyzers build && bun run --cwd packages/adapter-openclaw build && bun run --cwd packages/adapter-claude-code build && bun run --cwd packages/adapter-warp build && bun run --cwd packages/adapter-cline build && bun run --cwd packages/adapter-codex build",
    "test": "bun test",
    "test:core": "bun test packages/core",
    "test:db": "bun test packages/db",
    "dev:api": "bun run --cwd apps/api dev",
    "dev:web": "bun run --cwd apps/web dev",
    "release": "bun scripts/release.ts",
    "release:dry-run": "bun scripts/release.ts --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "keywords": [
    "llm",
    "ai",
    "agents",
    "cost",
    "optimization",
    "observability",
    "langfuse",
    "langsmith",
    "token",
    "multi-agent"
  ],
  "author": "vjvkrm",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vjvkrm/langcost.git"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "@types/bun": "^1.3.11",
    "@types/node": "^25.5.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3"
  }
}
